[package]
name = "flambeau-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Session, typed KV cache (F16/Q8/turbo), Mesh<N>, collectives, sampler, tokenizer glue, scheduler."

[dependencies]
flambeau-core = { path = "../core" }
flambeau-quant = { path = "../quant" }
# flambeau-ops is NOT a dependency: it depends on backend-hip, which depends
# on runtime, which would cycle. V1.7's mesh-generic trait lands in core.
anyhow.workspace = true
bytemuck.workspace = true
half.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true
tokenizers.workspace = true
minijinja.workspace = true

[lints]
workspace = true
