//! flambeau-backend-hip — `HipDevice`, `HipStream`, allocation, H↔D copies,
//! RCCL bindings for collectives, `KernelImpl<..., HipDevice>` registrations.
//!
//! V1.2: device + stream + RCCL. V1.3+: kernel-impl registrations land alongside
//! their `.cu` counterparts in `flambeau-kernels-hip`.
