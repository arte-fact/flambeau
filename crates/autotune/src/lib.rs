//! flambeau-autotune — T-track.
//! T1 shape recorder → T2 variant enumerator → T3 micro-bench runner →
//! T4 session integration → T5 auto-promote.
//! Runtime TUNING (selection between certed impls); never runtime CODEGEN.
//! Writes `dispatch/<backend>/<arch>.local.toml` (gitignored); committed table stays
//! the source of truth.
