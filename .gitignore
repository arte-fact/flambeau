# Rust build
/target
**/target
Cargo.lock.bak

# Machine-local autotuner overrides (T-track). Committed table is source of truth.
dispatch/**/*.local.toml

# rocprofv3 / Nsight traces
/profiles/
/.rocprofv3/
*.rocpd
*.csv.rocprof
*.json.rocprof
*.nsys-rep
*.ncu-rep

# Machine-local driver / rocprof env pins — use .env.example as the template.
.env

# Compiled kernel artefacts cached outside target/
*.hsaco
*.cubin
*.fatbin

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Python venvs (if any)
.venv/
venv/

# Claude Code runtime state
.claude/
tools/mtp_cache/
